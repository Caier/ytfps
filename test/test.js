const ftch = require('../out/index');
const chai = require('chai');
chai.use(require('chai-as-promised'));
const expect = chai.expect;

describe("sc", function() {
    this.timeout(30000);
    this.slow(10000);

    //tbd
});